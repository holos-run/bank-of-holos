apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: bank-of-holos
    environment: stage
    example.com/environment.name: stage
    example.com/owner.name: security
    example.com/tier.name: foundation
    holos.run/component.name: stage-bank-secrets
    team: security
    tier: foundation
  name: stage-bank-secrets
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: deploy/clusters/workload/components/stage-bank-secrets
    repoURL: https://github.com/jeffmccune/bank-of-holos
    targetRevision: main
