apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: bank-of-holos
    environment: dev
    example.com/environment.name: dev
    example.com/owner.name: security
    example.com/tier.name: security
    holos.run/component.name: dev-bank-namespaces
    team: security
    tier: security
  name: dev-bank-namespaces
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: deploy/clusters/workload/components/dev-bank-namespaces
    repoURL: https://github.com/jeffmccune/bank-of-holos
    targetRevision: main
