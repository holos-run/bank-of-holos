apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    argocd.argoproj.io/instance: istio-gateway
    holos.run/component.name: istio-gateway
  name: istio-gateway
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: deploy/clusters/workload/components/istio-gateway
    repoURL: https://github.com/holos-run/bank-of-holos
    targetRevision: main
