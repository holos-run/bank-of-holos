apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    labels:
        app.holos.run/cluster.name: workload
        app.holos.run/component.name: prod-east-secrets
        app.holos.run/environment.name: prod-east
        app.holos.run/project.name: bank-security
        app.holos.run/stack.name: bank-of-holos
        app.holos.run/team.name: security
    name: bank-security-prod-east-secrets
    namespace: argocd
spec:
    destination:
        server: https://kubernetes.default.svc
    project: bank-security
    source:
        path: deploy/clusters/workload/projects/bank-security/components/prod-east-secrets
        repoURL: https://github.com/jeffmccune/bank-of-holos.git
        targetRevision: main
