apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    application: bank-of-holos
    environment: stage
    example.com/environment.name: stage
    example.com/owner.name: backend
    example.com/tier.name: backend
    holos.run/component.name: stage-bank-contacts
    team: backend
    tier: backend
  name: stage-bank-contacts
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: stage-bank-backend
  source:
    path: examples/environments/deploy/clusters/workload/components/stage-bank-contacts
    repoURL: https://github.com/jeffmccune/bank-of-holos
    targetRevision: main
