apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    labels:
        app.holos.run/cluster.name: workload
        app.holos.run/component.name: ledger-writer
        app.holos.run/environment.name: test
        app.holos.run/project.name: test-bank-backend
        app.holos.run/stack.name: bank-of-holos
        app.holos.run/team.name: backend
    name: test-bank-backend-ledger-writer
    namespace: argocd
spec:
    destination:
        server: https://kubernetes.default.svc
    project: test-bank-backend
    source:
        path: deploy/clusters/workload/projects/test-bank-backend/components/ledger-writer
        repoURL: https://github.com/holos-run/bank-of-holos.git
        targetRevision: main
